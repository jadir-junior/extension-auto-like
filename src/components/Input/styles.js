import { COLORS } from "../../theme/theme";
import styled from "styled-components";

export const Wrapper = styled.input`
  border: none;
  font-size: 16px;
  padding: 16px;
  outline-color: ${COLORS.PRIMARY_COLOR};
  width: 100%;
`;
